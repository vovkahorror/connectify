import React, {FC} from 'react';

const ChatPage: FC = () => {
    return (
        <div>

        </div>
    );
};

const Chat: FC = () => {
    return (
        <div>

        </div>
    );
};

export default ChatPage;